{
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "example",
    "parcel-optimizer-twemoji-favicon"
  ],
  "devDependencies": {
    "prettier": "^2.8.1"
  }
}
